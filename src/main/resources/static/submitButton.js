const logoutBtn = document.getElementById("logout");
/*logoutBtn.addEventListener("click", (e) => {
   fetch("http://localhost8080:/logout");



    if(myChart1 != null) {
        destroyChart();
    }
    
    e.preventDefault();
    let userId = 0;
    userId = document.getElementById("userId").value;

    getData(userId);


});
*/